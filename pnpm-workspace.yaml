packages:
  - "apps/*"
  - "packages/*"

# pnpm 11/12 blocks dependency build scripts unless explicitly reviewed.
# esbuild is required by Vite/Vitest tooling and is intentionally approved.
allowBuilds:
  esbuild: true
