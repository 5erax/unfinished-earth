# Copy to .env for Docker Compose; generate a private code, never commit it.
WORLD_ACCESS_CODE=
# 30 = accelerated prototype (one game day per real minute).
# 1 = design timing (one game day per 30 real minutes).
SIM_SPEED=30
